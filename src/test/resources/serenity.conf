serenity.project.key = "saucedemo"

serenity.driver.capabilities {
  chromeOptions {
    args = [
      "--disable-notifications",
      "--start-maximized"
    ]
  }
}
    webdriver.driver = "chrome"

    webdriver {
        base.url = "https://www.saucedemo.com"
            }


report {
    outputDirectory = "target/serenity"
}
